apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-php-config
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-18"
data:
  POSTGRES_HOST: {{ quote .Values.postgresql.host }}
  POSTGRES_PORT: {{ quote .Values.postgresql.port }}
