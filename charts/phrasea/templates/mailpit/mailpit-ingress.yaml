{{- if and $.Values.stack.running .Values.mailpit.enabled }}
{{- if .Values.mailpit.ingress.enabled }}
{{- with .Values.mailpit }}
{{- $ingressNode := . }}
apiVersion: {{ template "ingress.apiVersion" $ }}
kind: Ingress
metadata:
  name: mailpit-ingress
{{- if .ingress.annotations }}
  annotations:
{{ toYaml .ingress.annotations | indent 4 }}
{{- end }}
{{- if .ingress.extraLabels }}
  labels:
{{ toYaml .ingress.extraLabels | indent 4 }}
{{- end }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ tpl (.hostname | required "Missing mailpit.hostname") $ }}
    http:
      paths:
      {{- include "ingress.rule_path" (dict "_" $ "name" "mailpit" "port" 8025) | indent 6 }}
{{- if .ingress.tls }}
{{- $tlsNode := .ingress.tls -}}
{{- range $value := $tlsNode }}
  {{- if not (hasKey $value "hosts") }}
    {{- $hosts := list $ingressNode.hostname }}
    {{- $_ := set $value "hosts" $hosts }}
  {{- end -}}
{{- end }}
  tls:
{{ toYaml $tlsNode | indent 2 }}
{{- else if $.Values.ingress.tls.wildcard.enabled }}
  tls:
  - hosts:
    - {{ tpl .hostname $ }}
    secretName: {{ include "secretRef.ingress.tls.wildcard" $ }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
